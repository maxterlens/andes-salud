/**
 * @NApiVersion 2.1
 * @NScriptType ClientScript
 * @NModuleScope SameAccount
 *
 * Client Script — Empleado
 * Lógica client-side del formulario de Empleado.
 * La funcionalidad será definida en una iteración posterior.
 */
define([], () => {

    const pageInit = (scriptContext) => {
        // TODO: definir lógica client-side
    };

    return { pageInit };
});
